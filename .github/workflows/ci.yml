name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Test with coverage
        run: npm run test:coverage
        env:
          CI: true

      - name: Build
        run: npm run build

      - name: Install GUI dependencies
        run: npm ci --prefix gui

      - name: Build GUI
        run: npm run build --prefix gui

      - name: Security audit (backend)
        run: npm audit --audit-level=high
        # Note: Set to 'high' to avoid failures from moderate vulnerabilities that require breaking changes
        # TODO: Upgrade vitest (esbuild fix) and pino (fast-redact fix) in a separate PR

      - name: Security audit (GUI)
        run: npm audit --audit-level=high --prefix gui
